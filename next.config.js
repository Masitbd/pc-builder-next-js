/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['daisyui.com', 'images.pexels.com', 'freepik.com'],
  },
}

module.exports = nextConfig
